Required Languages
    Python

make sure to install Flask 
    pip install flask

to run
    python app.py



Python → programming language

Flask → lightweight web framework (handles routes like /create-room and /join-room)

SQLite → built-in database (stores room codes and participants)

uuid (Python standard library) → generates unique room IDs/codes




------------------------------------------------------

Real-time updates with Flask-SocketIO.
    pip install flask-socketio
